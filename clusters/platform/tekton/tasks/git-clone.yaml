# Install Tekton Pipelines and Triggers on the platform cluster:
#   kubectl apply -f https://storage.googleapis.com/tekton-releases/pipeline/latest/release.yaml
#   kubectl apply -f https://storage.googleapis.com/tekton-releases/triggers/latest/release.yaml
#   kubectl apply -f https://storage.googleapis.com/tekton-releases/triggers/latest/interceptors.yaml
#
# Then apply these custom tasks and pipelines.
---
apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: git-clone
  namespace: tekton-builds
spec:
  params:
    - name: repo-url
      type: string
    - name: revision
      type: string
      default: main
  workspaces:
    - name: source
  steps:
    - name: clone
      image: alpine/git:2.43.0
      script: |
        git clone $(params.repo-url) $(workspaces.source.path)/source
        cd $(workspaces.source.path)/source
        git checkout $(params.revision)
        echo "Cloned $(params.repo-url) at $(params.revision)"
